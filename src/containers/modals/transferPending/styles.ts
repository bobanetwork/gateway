import styled from 'styled-components'

export const TransferPendingContainer = styled.div`
  display: flex;
  align-items: center;
  flex-direction: column;
  justify-content: space-around;
  gap: 50px 0px;
  img {
    width: 220px;
    height: auto;
    margin: -40px auto -60px auto;
  }
`
